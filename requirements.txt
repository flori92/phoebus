edge-tts>=7.0
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.0
google-genai>=1.0
openai>=1.0
Pillow>=10.0
pygame>=2.5
PyAutoGUI>=0.9
python-dotenv>=1.0
requests>=2.31
SpeechRecognition>=3.10
websockets>=12.0
chromadb>=0.4
scipy>=1.10

# ── Audio (micro) — cross-platform ────────────────────────────────────
# PyAudio nécessite portaudio :
#   macOS  : brew install portaudio  (géré par install.py / bootstrap.py)
#   Linux  : sudo apt install portaudio19-dev
#   Windows: install.py utilise pipwin pour installer un wheel pré-compilé
pyaudio>=0.2; platform_system != "Windows"

# ── Audio Robuste (Super-Pouvoir #1) ────────────────────────────────────
# VAD (Voice Activity Detection) professionnel — critique pour écoute sans écho
webrtcvad>=4.3

# Suppression bruit et silence intelligent (fallback VAD)
silero-vad>=5.0

# Audio file processing
soundfile>=0.12

# ── Wake Word — 100% gratuit, 100% local ──────────────────────────────
openwakeword>=0.6
# onnxruntime : version standard cross-platform
onnxruntime>=1.16; platform_system != "Windows"
# Sur Windows, onnxruntime standard ou DirectML (GPU) — les deux fonctionnent
onnxruntime>=1.16; platform_system == "Windows"
vosk>=0.3.44
pocketsphinx>=5.0

# ── STT local robuste ──────────────────────────────────────────────────
faster-whisper>=1.0

# ── Spotify ────────────────────────────────────────────────────────────
spotipy>=2.22

# ── Multi-utilisateurs (embeddings vocaux) ─────────────────────────────
resemblyzer>=0.1.1
python-telegram-bot>=20.0

# ── Windows-only helpers ───────────────────────────────────────────────
comtypes>=1.2; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"



